import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors
  static const Color primary = Color(0xFF006837);
  static const Color primaryLight = Color.fromRGBO(56, 154, 72, 0.3);

  // Error Color
  static const Color error = Colors.red;

  // Background
  static const Color containerBg = Color(0xFFF1F1F1);
  static const Color background = Color.fromRGBO(255, 255, 255, 1);
  // Divider
  static const Color divider = Color.fromRGBO(0, 0, 0, 0.2);

  // Text Colors
  static const Color textPrimary = Color(0xFF000000); // rgba(0, 0, 0, 1)
  static const Color textSecondary = Color.fromRGBO(0, 0, 0, 0.5);

  // TextField Border
  static const Color textFieldBorder = Color.fromRGBO(217, 217, 217, 0.25);
}
